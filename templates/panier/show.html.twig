{% extends 'base.html.twig' %}

{% block title %}{{ app_name }}{% endblock %}
{% block body %}
<!-- Page Content -->
    <div class="container">
        <div class="row">
            {% if(cart|length) %}
                <table class="table table-hover">
                    <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nom</th>
                        <th scope="col">Quantité</th>
                        <th scope="col">Handle</th>
                        <th scope="col"></th>
                    </tr>
                    </thead>
                    <tbody>
                    <!-- TODO loop on cart to display product's cart -->
                    <tr>
                        <th scope="row">1</th>
                        <td>Mark</td>
                        <td>Otto</td>
                        <td>@mdo</td>
                        <td>
                            <a href="{{ path('deleteAllCart') }}" type="submit" class="btn alert-primary">
                                <i class="fa fa-trash-o"></i>
                            </a>
                        </td>
                    </tr>
                    </tbody>
                </table>
            {% else %}
                <p class="alert-warning">Votre panier est vide !</p>
            {% endif %}
        </div>
    </div>
{% endblock %}